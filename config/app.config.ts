'use strict';

const API_URL = 'http://localhost:3003/api/';
const LANG = 'ru';
const COUNTRY = 'RU';

export const config = {
  apiUrl: API_URL,
  apiCatalog: '/api/catalog',
  language: LANG,
  country: COUNTRY,
}